import { defineConfig } from 'vite'

export default defineConfig({
    // IMPORTANT: Replace 'rehab-portfolio' below with your GitHub repo name
    // e.g. if your repo is github.com/Rehab-Sobhy/my-portfolio, set base: '/my-portfolio/'
    base: '/rehab-portfolio/',
})
